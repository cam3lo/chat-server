Chat server application written in Python that is functional at the moment although it still needs some tweaks to improve message sending. Next step is to have server source to run on Raspberry Pi so users can connect from separate machines.
